# LiveSpyer 开发约定

## 项目目标

使用 Java 开发直播网站桌面客户端：解析直播间的 HLS 播放地址（例如 `index.m3u8`），在软件界面内播放，并支持二维码登录、弹幕收发、音量调节等常规观看功能。界面文案和开发沟通默认使用简体中文。

## 当前工程

- 构建工具：Gradle 8.14，使用仓库自带的 Gradle Wrapper。
- 项目坐标：`top.vrilhyc.applications:LiveSpyer:1.0-SNAPSHOT`。
- 入口：`src/main/java/top/vrilhyc/applications/Main.java`，启动多房间桌面界面。
- 测试依赖：JUnit Jupiter 5.10.0，已启用 JUnit Platform。
- Java toolchain：JDK 21；界面：Swing + FlatLaf；播放器：Edge WebView2 + hls.js 1.5.15，当前支持 Windows x64，不使用 VLC。
- 界面主题集中在 `UiTheme`：粉蓝配色、半透明卡片、柔和渐变与有限交互动效；观看页为视频/消息分栏。实际 backdrop-filter 模糊仅用于 WebView2 视频浮层，Swing 渐变背景不冒充系统桌面模糊。保留减少动画偏好及最小窗口可用性。
- 支持收起消息栏、专注观看（双击视频）、F11 全屏与 Esc 返回；这些布局操作不得重建播放器或关闭游客订阅，消息栏宽度应能还原。
- 在线列表默认前 3 名、展开最多 50 名，来源是平台公开在线榜单，不宣称完整观众名册。已播时长使用平台开播时间，每秒更新；元数据与榜单每 30 秒匿名刷新，关闭房间取消任务，不借用账号凭据。
- 仅音频模式优先独立音频 HLS；未加密 fMP4/AAC 可通过严格 HTTP Range 仅读元数据和音频采样并重封装，禁止静默回退完整视频下载。切换前验证音轨及范围读取，失败保留原模式，保留音量、静音和游客订阅。混合 TS/加密流等不支持情况明确提示。
- 已实现平台扩展接口、Bilibili HLS 取流、扫码登录流程、多房间管理及发送后断开的临时互动连接。
- 已实现独立游客弹幕接收，在账号 disconnected 时显示视频覆盖弹幕及消息列表；不复用账号凭据，用户可关闭订阅。
- 游客消息支持公开昵称、荣耀等级及 SEND_GIFT / SEND_GIFT_V2 礼物文字；服务端打码名称不保证还原，等级缺失时不猜测，不以粉丝牌或舰队等级代替荣耀等级。
- 用户已授权独立游客会话自动刷新：默认开启，每 15 秒刷新，收到打码弹幕立即刷新；连续 3 次无效暂停但保持接收。刷新不操作账号或媒体连接，不承诺恢复全名。
- 支持直接粘贴 m3u8 地址；Java 本地媒体转发处理 Referer、跨域和播放列表引用，浏览器只显示本地播放器。
- 已验证离线测试、公开 HLS、二维码生成及未扫码轮询、Windows 浏览器内嵌真实画面、直链对话框、全屏切换、静音命令和停止；声音听感及真实账号互动尚待联调。详见 `README.md`。

## 技术与目录约定

- 业务逻辑使用 Java，沿用现有 Gradle 工程和 `top.vrilhyc.applications` 根包。
- 开始实现时明确 JDK 版本并配置 toolchain，确认与 Gradle、UI 框架及播放引擎兼容。
- 当前沿用参考项目的 Swing 界面，通过独立 `LivePlayer` 接口封装 WebView2；Windows 宿主仅负责窗口嵌入，业务逻辑继续使用 Java。替换播放引擎前验证实际 HLS 编解码与生命周期。
- 按实际需求逐步建立 `ui`、`platform`、`player`、`auth`、`danmaku`、`model`、`config` 包，避免把所有功能堆入 `Main`。
- 平台适配层负责房间信息、播放地址、登录与弹幕协议；UI 通过服务接口调用，不直接实现平台协议。
- 测试放在 `src/test/java`，资源放在 `src/main/resources`。
- 新增依赖必须有明确用途，使用固定版本；不得提交构建缓存或本机 IDE 配置变更。

## 功能要求

### 直播源解析与播放

- 接收直播间 URL 或房间号，通过目标平台实际可用的接口或页面数据解析播放地址。
- 不假定播放列表文件名固定为 `index.m3u8`；支持带签名和时效参数的 URL，以及主播放列表与媒体播放列表。
- 播放请求按平台要求传递必要的 Cookie、Referer、User-Agent 等信息，并避免向无关域名转发凭据。
- 当前低延迟策略：hls.js lowLatencyMode，目标 1 个分片、超过 3 个分片时追赶，最高 1.1 倍速；优先 H.264 fMP4，无兼容流时回退 TS。不得把缓存参数等同于总延迟或承诺亚秒延迟。
- 提供“回到直播”和播放列表直播边缘距离估计；该估计不包含源头延迟，也不等于与网页的差值。
- 直链模式不关联账号或房间互动；保留签名查询参数，不将账号 Cookie 传入浏览器。转发服务仅监听 127.0.0.1，使用随机令牌和已登记的媒体引用，禁止任意目标代理及跳转网页。
- 播放地址失效时重新解析；区分未开播、房间不存在、登录失效、网络错误与媒体格式不支持等情况。
- 视频嵌入客户端界面；提供播放/停止、静音、音量调节、全屏及加载/错误状态。平台支持时再添加清晰度切换。
- 切换房间或关闭窗口时释放播放器、连接、定时任务和相关资源。

### 二维码登录

- 实现平台真实的二维码生成、展示、状态轮询、扫码确认、过期刷新和取消流程。
- 轮询应有超时、合理间隔和取消机制，登录成功后停止轮询。
- 管理登录状态与退出登录；如需持久化会话，使用系统凭据存储或合理的受保护存储方案。
- Cookie、Token、二维码登录密钥不得写入源码、版本库或日志。

### 弹幕与交互

- 默认策略：取流不建立互动连接；发送弹幕时 connect，等待鉴权确认，发送完成或失败后均 disconnected。播放连接独立存活。
- 发送前校验登录状态与输入内容；账号发送连接不得加入常驻心跳或自动重连。
- 游客弹幕是独立可关闭的订阅：uid=0，仅使用匿名设备标识，不接受账号会话参数；允许接收心跳和最多 3 次退避重连，不得回退使用登录凭据。取消显示或关闭房间即关闭游客连接。
- 定期游客会话刷新与网络异常重连分开计数；可关闭刷新，关闭房间或订阅须取消待完成的刷新，不把无消息或礼物全名判为弹幕全名恢复。
- 遵守平台发送频率限制；界面明确显示发送结果，不把本地回显当作服务端发送成功。
- 关闭房间停止账号与游客连接；退出登录停止账号连接，独立游客订阅可继续。异步消息通过订阅代次校验避免旧消息串入新订阅。媒体重试有次数上限，发送请求不得自动重发。
- 发送失败保留输入；游客弹幕的帧、解压大小、队列、去重历史及覆盖层数量均需设上限，用户内容只按纯文本显示。
- `disconnected` 只表示本客户端连接关闭，不得向用户承诺平台在线名单即时消失或完全匿名。
- 界面中的账号互动 disconnected 与游客弹幕连接状态分开显示；平台仍可能把游客接收连接计为在线。直链未绑定房间号时不提供弹幕订阅。

## 并发与错误处理

- HTTP 请求、播放地址解析、登录轮询和弹幕连接不得阻塞 UI 线程；界面更新必须回到对应 UI 线程。
- 网络请求设置超时，后台任务可取消；不要无限重试，也不要对弹幕发送等有副作用的请求盲目重试。
- 错误信息应便于用户理解；诊断日志保留必要上下文，但隐藏凭据和带签名的完整播放 URL。
- 房间切换和退出登录时处理异步结果过期问题，避免旧请求覆盖当前界面状态。

## 构建与验证

- Windows：`./gradlew.bat build`、`./gradlew.bat test`。
- macOS/Linux：`./gradlew build`、`./gradlew test`。
- 启动：`./gradlew.bat run`；分发：`./gradlew.bat installDist`。需要 Windows x64、JDK 21、.NET Framework 4.8 和 Edge WebView2 Runtime。Gradle 自动构建浏览器宿主；IDE 首次运行前执行 `buildBrowserHost`。
- 宿主源文件位于 `native/windows`，SDK 与产物在忽略的 `build/`，固定版本并校验下载哈希，分发保留许可证。原生宿主查找须兼容非应用目录启动。不再打包或加载 VLC。
- `./gradlew.bat packageWindows` 生成 Windows 11 x64 的 EXE 安装包和便携 ZIP，内置 Temurin JRE 21.0.12.1+1 与 WebView2 Fixed Runtime 153.0.4234.48。打包使用独立宿主目录，不覆盖运行中的开发版。目标用户无需配置 Java 等开发环境。
- 打包产物、中间文件及专用缓存集中于 `build/packaging/`，最终包位于 `build/packaging/distributions/`，应用目录为 `build/packaging/app-版本/LiveSpyer/`。`installDist` 位于 `build/packaging/install/LiveSpyer/`；根目录 `packaging/` 仅保存版本管理中的源配置。
- 可选联网检查：`./gradlew.bat liveSmoke -PliveRoom=6`；默认测试不联网，联网检查不登录真实账号或发送弹幕。
- 对播放地址解析、登录状态转换、协议解码与异常处理编写有意义的单元测试；默认测试不依赖真实账号或在线直播间。
- 接入实际平台后手动验证扫码成功/过期、直播播放、音量/静音、弹幕收发、房间切换、断线恢复和退出清理。
- 提交工作结果时说明修改内容、执行过的验证及尚未验证的外部依赖；不能把模拟数据验证表述为真实平台联调成功。

## 当前接入范围与待验证项

- 当前仅适配 Bilibili；通过 `LivePlatform` 和 `PlatformRegistry`/ServiceLoader 扩展平台。
- Windows x64 已验证浏览器加载、真实 HLS 画面、直链输入、全屏与停止；其他系统尚无窗口宿主，声音听感、扫码成功和真实弹幕发送仍需联调。
- 参考工程只读：`E:\save\webs\Demo Projects\Demo Project\demo`。不得复用或输出其账号凭据、浏览器配置和会话。
- 平台相关实现应核实实际接口，不能把模拟测试或 HTTP 播放列表检查表述为完成真实账号/音视频联调。
